<!DOCTYPE html>
<html>
  <head>
    <title>{{ title }}</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <img src="{{image_url}}" />
    <h2>{{ name }}</h2>
    phone: {{ display_phone }}<br />
    {{#location.display_address}}
      {{.}}<br />
    {{/location.display_address}}
  </body>
</html>
